{
  "name": "presence-theme-vite",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "pack": "pnpm build && node ./scripts/pack.mjs"
  },
  "devDependencies": {
    "vite": "^6.0.0"
  }
}
